/**
 * Middle of the 1–5 scale — the check-in starts here so the user just nudges from neutral.
 * (Score→colour mapping retired with brand v1: dots are ink; state colour exists only at the
 * Verdict, from `STATE`/`CORAL_DOTS` in `brand.ts`. docs/DESIGN.md §2.)
 */
export const DEFAULT_SCORE = 3;
